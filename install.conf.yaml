- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
  ~/.aliases:
  # ~/.bash_profile:
  # ~/.bashrc:
  # ~/.tmux.conf:
  # ~/.vimrc:
  # ~/.zshrc:  

# - shell:
  # - git submodule sync --recursive
  # - git update-submodules # after linking ~/.gitconfig